# Viberational

Viberational website source code.

## Getting Started

Install dependencies and start the dev server:

```bash
pnpm install
pnpm dev
```

## Notes

- The app runs at `http://localhost:3000`.
- Use `pnpm build` for production builds and `pnpm start` to serve them.
